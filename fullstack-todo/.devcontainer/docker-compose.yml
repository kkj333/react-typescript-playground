version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    ports:
      - "5173:5173"  # Frontend (Vite)
      - "8000:8000"  # Backend (FastAPI)
